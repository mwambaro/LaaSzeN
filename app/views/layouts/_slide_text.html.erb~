    
    <div id="slides">   
        <% @count = @count_slides %>
        <% unless @count_slides <= 1 %>
            <div id="prev_next_slide_1" class="block">
                <i id="prev_slide_top" class="fa fa-chevron-circle-left fa-3x"></i>
                <i id="next_slide_top" class="fa fa-chevron-circle-right fa-3x"></i>
            </div>
        <% end %>
        
        <div id="slide_content" class="block">
            <% @model_name = 'Slide' %>
            <% @model = @slide %>
            <%= render 'layouts/format_slide_text' %> 
        </div>
        
        <% unless @count_slides <= 1 %>
            <div id="prev_next" class="block">
                <i id="prev_slide_bottom" class="fa fa-chevron-circle-left fa-3x"></i>
                <i id="next_slide_bottom" class="fa fa-chevron-circle-right fa-3x"></i>
            </div>
        <% end %>
    </div>
