<%= form_for(@intro_text) do |f| %>
  <% if @intro_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@intro_text.errors.count, "error") %> prohibited this intro_text from being saved:</h2>

      <ul>
      <% @intro_text.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :language %><br>
    <%= f.text_field :language %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.file_field :upload_intro_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
