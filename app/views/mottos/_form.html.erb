<%= form_for(@motto) do |f| %>
  <% if @motto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@motto.errors.count, "error") %> prohibited this motto from being saved:</h2>

      <ul>
      <% @motto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :language %><br>
    <%= f.text_field :language %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <% if @motto.content %>
        <%= f.text_area :content, value: @motto.content.to_s, col: 80, row: 'auto', readonly: true %>
    <% end %>
    <%= f.file_field :upload_motto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
